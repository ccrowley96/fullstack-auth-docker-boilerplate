export const themes = {
    light: 'light',
    dark: 'dark'
}

export const actionTypes = {
    // Redux style action type keys here, example action for setting the active modal
    // SET_ACTIVE_MODAL: 'SET_ACTIVE_MODAL'
}